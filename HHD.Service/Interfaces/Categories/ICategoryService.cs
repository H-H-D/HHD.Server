using HHD.Service.DTOs.Categories;
using System.Linq.Expressions;

namespace HHD.Service.Interfaces.Categories;

public interface ICategoryService
{
    //IQueryable<CategoryForResultDto> Get(
    //Expression<Func<CategoryForCreationDto, bool>>? predicate = default,
    //bool asNoTracking = false);

    //ValueTask<User?> GetByIdAsync(
    //Guid userId,
    //bool asNoTracking = false,
    //CancellationToken cancellationToken = default);

    //ValueTask<User> CreateAsync(
    //User user,
    //bool saveChanges = true,
    //CancellationToken cancellationToken = default);

    //ValueTask<User> UpdateAsync(
    //User user,
    //bool saveChanges = true,
    //CancellationToken cancellationToken = default);

    //ValueTask<User?> DeleteAsync(
    //Guid userId,
    //bool saveChanges = true,
    //CancellationToken cancellationToken = default);
}
